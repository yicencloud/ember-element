<div
  id="tab-{{@pane.name}}"
  aria-controls="pane-{{@pane.name}}"
  role="tab"
  aria-selected="true"
  class="el-tabs__item is-{{@pane.parent.position}} {{if @pane.active "is-active"}} {{if (or @pane.parent.closable @pane.parent.editable) "is-closable"}}"
  {{on 'click' (fn this.handleClick)}}
  {{did-insert this.handleDidInsertOrUpdate}}
  {{did-update this.handleDidInsertOrUpdate}}
>
  {{@pane.label}}
  {{#if (or @pane.parent.closable @pane.parent.editable)}}
    <span class="el-icon-close" {{on "click" (fn this.handleClose)}}></span>
  {{/if}}
</div>